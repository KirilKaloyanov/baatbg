export interface RegionDTO {
  id: string;
  imgThumb: string;
  imgHero: string;
  header: {
    en: string;
    bg: string;
  };
  heading: {
    en: string;
    bg: string;
  };
  text: {
    en: string;
    bg: string;
  };
}
